Flask==2.3.3
# Intentionally vulnerable dependencies for SCA testing (compatible versions)
urllib3==1.24.1  # Known vulnerabilities - has pre-built wheels
requests==2.20.0  # Known vulnerabilities - has pre-built wheels
pyyaml==5.1  # Known vulnerabilities - has pre-built wheels
jinja2==3.1.2  # Compatible with Flask 2.3.3
markupsafe==2.1.1  # Compatible with werkzeug 3.1.3
# Removed problematic packages that require compilation:
# cryptography, pillow, django, lxml - these cause build failures